/// <reference types="react" />
export declare const LogoField: ({ record, source, size, }: {
    record?: {
        logo: string;
        name: string;
    };
    source?: string;
    size?: 'small' | 'medium';
}) => JSX.Element;
