/// <reference types="react" />
import { Company } from '../db/types';
export declare const CompanyAvatar: ({ record, size, }: {
    record?: Company;
    size?: 'small' | 'large';
}) => JSX.Element;
